# Server
NODE_ENV=development
PORT=8000
API_PREFIX=/api/v1

# MongoDB Atlas
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/reyan_luxe?retryWrites=true&w=majority

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5173,https://your-frontend-domain.com

# JWT (Phase 3 — required before production)
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars-production
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Razorpay (Phase 6)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# Google OAuth (Phase 3)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Email (Phase 3+)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
EMAIL_FROM=noreply@reyanluxe.com

# Frontend URL (password reset links, etc.)
FRONTEND_URL=https://your-frontend-domain.com
FRONTEND_BASE_PATH=/

# Admin
ADMIN_API_KEY=your-admin-api-key-change-me-production
ADMIN_SEED_EMAIL=admin@reyanluxe.com
